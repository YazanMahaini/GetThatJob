# Approved CV index

List every original CV in `CVs/`, its format, approval status, factual role, and best-fit job types. Do not edit the originals. Read all approved CVs for supported facts, while resolving conflicts against official evidence and direct user corrections.

## Visual design reference

- Chosen CV design reference (filename in `CVs/`): Not selected

If there is one approved CV, use its design. If several approved CVs share a design, record one representative filename. If their designs differ, ask the applicant to choose **one of their own CVs** as the visual design reference and record its filename here. Keep that choice until the applicant changes it. This choice controls visual styling only; it does not limit which approved CVs supply facts. Never use the plugin creator's or another applicant's CV as a reference.

| File in CVs/ | Approved? | Format | Use and factual notes |
| --- | --- | --- | --- |

Preserve the chosen design reference's typography, spacing, ordering, and layout where practical unless the applicant requests a redesign. Match the employer's requested output format; create a Word file only when the applicant or application requires one.
