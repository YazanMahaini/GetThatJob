# Approved CV index

List each original CV in `CVs/`, its format, approval status, factual role, and best-fit job types. Do not edit the originals. Choose the closest user-approved CV for a posting and tailor a copy in the application packet.

| File in CVs/ | Approved? | Format | Use and factual notes |
| --- | --- | --- | --- |

The source CV controls the new CV's visual style unless the user requests a redesign. Preserve its typography, spacing, ordering, and layout where practical. Match the employer's requested output format; create a Word file only when the user or application requires one. Never use another applicant's CV as a style reference.
